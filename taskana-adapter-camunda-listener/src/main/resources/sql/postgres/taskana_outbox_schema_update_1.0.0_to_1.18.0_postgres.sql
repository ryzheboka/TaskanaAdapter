-- this script updates the tables OUTBOX_SCHEMA_VERSION and event_store.
SET search_path TO %schemaName%;

INSERT INTO OUTBOX_SCHEMA_VERSION (VERSION, CREATED) VALUES ('1.18.0', CURRENT_TIMESTAMP);

ALTER TABLE event_store ADD COLUMN LOCK_EXPIRES TIMESTAMP;
